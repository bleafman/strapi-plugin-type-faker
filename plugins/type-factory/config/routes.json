{
  "routes": [
    {
      "method": "GET",
      "path": "/",
      "handler": "typeFactory.index",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/health-check",
      "handler": "typeFactory.healthCheck",
      "config": {
        "policies": []
      }
    }
  ]
}
